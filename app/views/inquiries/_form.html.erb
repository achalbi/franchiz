<%= form_for(@inquiry) do |f| %>
  <% if @inquiry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inquiry.errors.count, "error") %> prohibited this inquiry from being saved:</h2>

      <ul>
      <% @inquiry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :token %>
    <%= f.hidden_field :business_id %>

      <%= f.fields_for :user do |user_field| %>
<!--        <% if @inquiry.user.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@inquiry.user.errors.count, "error") %>  found, please fill up highlighted fields:</h2>
      
            <ul>
            <% @inquiry.user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>-->
      
        <div class="form-inline">
            <div class="form-group">
              <%= user_field.label :salutation, class: "sr-only"%><br>
              <%= user_field.select :salutation, options_for_select([ "Mr", "Ms" ], "Mr"),{}, {class: "form-control"} %>
            </div>
            <div class="form-group">
              <%= user_field.label :fname, class: "sr-only" %><br>
              <%= user_field.text_field :fname, class: "form-control", placeholder: "First Name" %>
            </div>
            <div class="form-group">
              <%= user_field.label :lname, class: "sr-only" %><br>
              <%= user_field.text_field :lname, class: "form-control", placeholder: "Last Name" %>
            </div>
        </div>
        <div class="form-inline">
          <div class="form-group">
            <%= user_field.label :email, class: "sr-only" %><br>
            <%= user_field.text_field :email, class: "form-control width-100", placeholder: "e-mail" %>
          </div>
          <div class="form-group">
            <%= user_field.label :mobile, class: "sr-only" %><br>
            <%= user_field.text_field :mobile, class: "form-control width-100", placeholder: "mobile" %>
          </div>
        </div>
        <br>
            <%= user_field.fields_for :address do |address_field| %>
            <!--        
              <% if @inquiry.user.address.errors.any? %>
                <div id="error_explanation">
                  <h2><%= pluralize(@inquiry.user.address.errors.count, "error") %> prohibited this address from being saved:</h2>
            
                  <ul>
                  <% @inquiry.user.address.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                  </ul>
                </div>
              <% end %>
              -->
            
              <div class="form-group hidden-lg-down">
                <%= address_field.label :doorno, class: "sr-only" %><br>
                <%= address_field.text_field :doorno %>
              </div>
              <div class="form-group hidden-lg-down">
                <%= address_field.label :street, class: "sr-only" %><br>
                <%= address_field.text_field :street %>
              </div>
              <div class="form-group">
                <%= address_field.label :city, class: "sr-only" %>
                <%= address_field.text_field :city, class: "form-control", placeholder: "City" %>
              </div>
              <div class="form-group hidden-lg-down">
                <%= address_field.label :state, class: "sr-only" %><br>
                <%= address_field.text_field :state %>
              </div>
              <div class="form-group hidden-lg-down">
                <%= address_field.label :country, class: "sr-only" %><br>
                <%= address_field.text_field :country %>
              </div>
              <div class="form-group hidden-lg-down">
                <%= address_field.label :pincode, class: "sr-only" %><br>
                <%= address_field.text_field :pincode %>
              </div>
              <div class="form-group hidden-lg-down">
                <%= address_field.label :latitude, class: "sr-only" %><br>
                <%= address_field.text_field :latitude %>
              </div>
              <div class="form-group hidden-lg-down">
                <%= address_field.label :longitude, class: "sr-only" %><br>
                <%= address_field.text_field :longitude %>
              </div>
            <% end %>
      <% end %>




  <div class="actions">
    <%= f.submit class: "btn btn-primary"  %>
  </div>
<% end %>
