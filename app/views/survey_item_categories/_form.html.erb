<%= form_for(@survey_item_category) do |f| %>
  <% if @survey_item_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_item_category.errors.count, "error") %> prohibited this survey_item_category from being saved:</h2>

      <ul>
      <% @survey_item_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :categorisable_id %><br>
    <%= f.text_field :categorisable_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
