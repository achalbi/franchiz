  
<%= form_for(@survey_question) do |f| %>
  <% if @survey_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_question.errors.count, "error") %> prohibited this survey_question from being saved:</h2>

      <ul>
      <% @survey_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title, class: "sr-only" %>
    <%= f.text_field :title, :disable_with => "Updating...", class: "form-control", placeholder: "Write question here..."  %>
  </div>
    <%= f.hidden_field :business_id %>
    <%= f.submit class: "btn btn-primary" %>
    <%= link_to "Delete", @survey_question,  method: :delete, class: "btn btn-danger", data: {confirm: "Are you sure?"} %>
<% end %>