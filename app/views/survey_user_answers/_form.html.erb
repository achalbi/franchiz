<%= form_for(@survey_user_answer) do |f| %>
  <% if @survey_user_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_user_answer.errors.count, "error") %> prohibited this survey_user_answer from being saved:</h2>

      <ul>
      <% @survey_user_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
