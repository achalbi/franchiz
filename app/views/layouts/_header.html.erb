<nav class="navbar navbar-full navbar-primary bg-faded">
<div class="container" >
  <a class="navbar-brand" href="/">Franchiz</a>
  <!--
  <a id="trigger" class="navbar-brand" type="button">Franchise</a> 
  class="trigger-button"  -->
  <div id="franchiz-content"></div>
  <ul class="nav navbar-nav">
    <% unless logged_in? %>
    <li class="nav-item pull-xs-right">
          <%= link_to "Sign up Business", new_business_path, class: "nav-link" %>
    </li>
    <% else %>
    <li class="nav-item dropdown pull-xs-right mr15">
    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.name %></a>
    <div class="dropdown-menu">
      <%= link_to "Profile", business_biz_user_path(session[:business_id],current_user), class: "dropdown-item" %>
      <div class="dropdown-divider"></div>
      <%= link_to "Sign out", session_path, method: :delete, class: "dropdown-item" %>
    </div>
    </li>
    <% end %>
  </ul>
</div>
</nav>