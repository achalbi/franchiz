<%= form_for(@inquiry_filter) do |f| %>
  <% if @inquiry_filter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inquiry_filter.errors.count, "error") %> prohibited this inquiry_filter from being saved:</h2>

      <ul>
      <% @inquiry_filter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
